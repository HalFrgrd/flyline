# Copilot
flyline --ai-command \
    --system-prompt "Be concise. Answer with a JSON array of <=3 items with objects containing command and description. Command will be a bash command." \
    copilot --prompt --reasoning-effort low 

# Claude
